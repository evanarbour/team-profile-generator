
// dynamically create manager card based on user info

// dynamically create engineer card 

// dynamically create intern card 

// then put all of those cards into an array

// get 'teamArray' data from index.js and pass into a 'for loop' to make cards for each member added by user

generateHTML = (data) => {
    
}




module.exports = generateHTML;